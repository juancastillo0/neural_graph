targets:
  $default:
    builders:
      artemis:
        options:
          schema_mapping:
            - output: lib/api.graphql.dart
              schema: lib/schema.graphql
              queries_glob: lib/*/**.graphql
